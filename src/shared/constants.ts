/** Default separator used between groups of three digits (e.g. `1 234 567`). */
export const DEFAULT_THOUSANDS_SEPARATOR = ' '

/** Default separator between the integer and fractional part (e.g. `1234,56`). */
export const DEFAULT_DECIMAL_SEPARATOR = ','

/** Currency symbol for the Azerbaijani manat. */
export const AZN_SYMBOL = '₼'
